import { AppBar, Box } from '@mui/material'
import React from 'react'

const Navbar = () => {
    return
    <AppBar sx={{ bgcolor: "yellow", p: 2 }} position='sticky'>
        Navbar
    </AppBar>;
};

export default Navbar
