import { Box } from '@mui/material'
import React from 'react'

const Feed = ({flex}) => {
    
  return (
    
    <Box sx={{ bgcolor: "black",p:2,color:"white" }} flex={flex}>
      feed
    </Box>
  );
}

export default Feed
